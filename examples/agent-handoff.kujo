# An example shaped for the next agent: explicit names, one obvious path.
# Nothing clever to reverse-engineer before continuing the work.

func classify(score) {
    if score >= 90 {
        return "ship"
    }
    if score >= 70 {
        return "review"
    }
    return "hold"
}

print(classify(82))
